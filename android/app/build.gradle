plugins {
    id 'com.android.application'
}

android {
    namespace 'cz.dneskane.app'
    compileSdk 35

    defaultConfig {
        applicationId 'cz.dneskane.app'
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
